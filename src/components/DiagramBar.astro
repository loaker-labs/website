---
const  { title, price }  = Astro.props;
---

<div class="flex flex-col xl:flex-row place-content-center place-self-start">
    <div class="flex h-[60px] place-content-center place-self-start place-items-center bg-teal-400" style={`width:${price*80}px;`}>{title}</div>
    <div class="flex self-center">{price}M</div>
</div>